export default function Home() {
  return (
    <main className="shell">
      <div className="card">
        <h1>MystOS Owner Hub</h1>
        <p>
          The internal dashboard will live here in a future slice. For now this keeps routing
          stable while the public site launches first.
        </p>
      </div>
    </main>
  );
}

