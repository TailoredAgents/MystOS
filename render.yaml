services:
  - type: web
    name: myst-site
    env: node
    plan: starter
    region: virginia
    buildCommand: |
      set -euo pipefail
      export PNPM_HOME="$HOME/.local/share/pnpm"
      export PNPM_STORE_PATH="$HOME/.local/share/pnpm-store"
      mkdir -p "$PNPM_HOME" "$PNPM_STORE_PATH"
      export PATH="$PNPM_HOME:$PATH"
      export HUSKY=0
      npx -y pnpm@9.15.9 install --frozen-lockfile --reporter=append-only 2>&1 | tee pnpm-install.log
      install_status=${PIPESTATUS[0]}
      if [ "$install_status" -ne 0 ]; then
        find . -maxdepth 5 -name 'pnpm-debug.log' -o -name '.pnpm-debug.log';
        find . -maxdepth 5 -name 'pnpm-debug.log' -o -name '.pnpm-debug.log' -exec cat {} +;
        cat pnpm-install.log;
        exit $install_status;
      fi
      npx -y pnpm@9.15.9 --filter site build --reporter=append-only
    startCommand: |
      set -eux
      export PNPM_HOME="$HOME/.local/share/pnpm"
      mkdir -p "$PNPM_HOME"
      export PATH="$PNPM_HOME:$PATH"
      npx -y pnpm@9.15.9 --filter site start -- --hostname 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: DATABASE_URL
        fromDatabase:
          name: mystos-db
          property: connectionString
      - key: NEXT_PUBLIC_SITE_URL
        sync: false
      - key: NEXT_PUBLIC_GA4_ID
        sync: false
      - key: NEXT_PUBLIC_META_PIXEL_ID
        sync: false
      - key: NEXT_PUBLIC_API_BASE_URL
        sync: false
    buildFilter:
      paths:
        - apps/site/**
        - packages/**
        - package.json
        - pnpm-lock.yaml
        - turbo.json
  - type: web
    name: myst-api
    env: node
    plan: starter
    region: virginia
    buildCommand: |
      set -euo pipefail
      export PNPM_HOME="$HOME/.local/share/pnpm"
      export PNPM_STORE_PATH="$HOME/.local/share/pnpm-store"
      mkdir -p "$PNPM_HOME" "$PNPM_STORE_PATH"
      export PATH="$PNPM_HOME:$PATH"
      export HUSKY=0
      npx -y pnpm@9.15.9 install --frozen-lockfile --reporter=append-only 2>&1 | tee pnpm-install.log
      install_status=${PIPESTATUS[0]}
      if [ "$install_status" -ne 0 ]; then
        find . -maxdepth 5 -name 'pnpm-debug.log' -o -name '.pnpm-debug.log';
        find . -maxdepth 5 -name 'pnpm-debug.log' -o -name '.pnpm-debug.log' -exec cat {} +;
        cat pnpm-install.log;
        exit $install_status;
      fi
      npx -y pnpm@9.15.9 --filter api build --reporter=append-only
    startCommand: |
      set -eux
      export PNPM_HOME="$HOME/.local/share/pnpm"
      mkdir -p "$PNPM_HOME"
      export PATH="$PNPM_HOME:$PATH"
      npx -y pnpm@9.15.9 --filter api start -- --hostname 0.0.0.0 --port $PORT
    preDeployCommand: |
      set -eux
      export PNPM_HOME="$HOME/.local/share/pnpm"
      mkdir -p "$PNPM_HOME"
      export PATH="$PNPM_HOME:$PATH"
      npx -y pnpm@9.15.9 -w db:migrate
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: DATABASE_URL
        fromDatabase:
          name: mystos-db
          property: connectionString
      - key: GA4_MEASUREMENT_ID
        sync: false
      - key: GA4_API_SECRET
        sync: false
    buildFilter:
      paths:
        - apps/api/**
        - packages/**
        - drizzle.config.ts
        - package.json
        - pnpm-lock.yaml
        - turbo.json
  - type: redis
    name: myst-redis
    plan: starter
    region: virginia
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all Render services (adjust as needed)
databases:
  - name: mystos-db
    plan: basic-1gb
    region: virginia
    databaseName: mystos
    user: myst
    ipAllowList: []
